# Required Environment Variables for Reframe Agents

# Google AI API Key (REQUIRED)
# Get from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your-google-api-key-here

# Langfuse Configuration (REQUIRED for prompt management)
# Get from: https://langfuse.com/
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_PUBLIC_KEY=your-public-key
LANGFUSE_SECRET_KEY=your-secret-key

# Optional: Supabase for persistent storage
# If not provided, uses in-memory storage
SUPABASE_REFRAME_DB_CONNECTION_STRING=postgresql://user:password@host:port/database

# Optional: Arize for monitoring
ARIZE_SPACE_ID=your-space-id
ARIZE_API_KEY=your-api-key

# Optional: GCS for artifact storage
# Defaults to "re-frame" if not specified
GCS_BUCKET_NAME=re-frame

# Development Settings (optional)
DEBUG=true
LOG_LEVEL=INFO